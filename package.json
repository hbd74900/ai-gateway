{
  "name": "ai-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "node server.js",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.23.0"
  },
  "dependencies": {
    "eventsource-parser": "^3.1.1"
  }
}
